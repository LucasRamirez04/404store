server:
  port: 8087

spring:
  application:
    name: microservicio-promocion
  datasource:
    url: jdbc:mysql://localhost:3306/db_404store_promocion
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    baseline-on-migrate: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
  instance:
    preferIpAddress: true

logging:
  level:
    root: INFO
    _store.promocion: INFO
  file:
    name: logs/promocion.log